-- Typography sub-report query
-- This section uses static typography showcase content. No data binding needed.
-- The typography features are CSS-driven, not data-driven.

SELECT 'Typography showcase' AS title FROM DUAL
